1. In order to compile and run the program have JDK version 13 and up installed
2. Run the following command in the same directory as PrimeFinder.java:  
3. javac PrimeFinder.java
4. java PrimeFinder
